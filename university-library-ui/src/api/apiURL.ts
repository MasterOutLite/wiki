const baseApi = 'http://localhost:3030';
const loginURL = baseApi + '/auth/login';
const logoutURL = baseApi + '/auth/logout';
const getInfoURL = baseApi + '/auth/getInfo';
const usersURL = baseApi + '/users';
const postsURL = baseApi + '/posts';

export {
  baseApi,
  loginURL,
  logoutURL,
  getInfoURL,
  usersURL,
  postsURL,
};
